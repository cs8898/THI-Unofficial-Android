<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/thiblue</item>
        <item name="colorPrimaryDark">@color/thibluedarker</item>
        <item name="colorAccent">@color/choosen_accent</item>
        <item name="titleTextColor">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AboutTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/thiblue</item>
        <item name="colorPrimaryDark">@color/thibluedarker</item>
        <item name="colorAccent">@color/thiyellowit</item>
        <item name="titleTextColor">@color/white</item>
    </style>
</resources>